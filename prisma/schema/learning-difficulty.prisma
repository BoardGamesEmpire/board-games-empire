enum LearningDifficulty {
  Challenging // Required multiple explanations
  Easy // Most people got it quickly
  Moderate // Took some explanation but then understood
  VeryDifficult // Still confusion after multiple plays
  VeryEasy // Everyone picked it up immediately

  @@map("learning_difficulties")
}
