model SystemSetting {
  id                    String  @id @default(cuid(2))
  allowPasswordResets   Boolean @default(true)
  allowUserRegistration Boolean @default(true)
  allowUsernameChange   Boolean @default(true)

  @@map("system_settings")
}
