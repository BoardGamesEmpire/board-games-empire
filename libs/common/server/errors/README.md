# ServerErrors

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ServerErrors` to build the library.

## Running unit tests

Run `nx test ServerErrors` to execute the unit tests via [Jest](https://jestjs.io).
